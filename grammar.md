# 语法累积表

**平时不看。** 只在换「本周只盯」的时候开一次，挑出现次数最多的那一条。

语法不靠意志力改，靠 `phrases.md` 每周三遍的重复自然掉。这张表的唯一用途是**告诉你哪一条最值得盯**。

---

## 本周只盯

> **we're given**（被动语态）

一次只盯一条。说话时能同时监控的错误项只有一个——盯三个等于一个都盯不住，而且会让语速变慢、填充词变多。

---

## 累积表

| 中文意思 | 正确说法 | 次数 | 首次 |
|---|---|---|---|
| 题目给了我们一个… | **we're given** a / an … | 1 | 01 |
| 按升序排好的 | sorted **in** ascending order | 1 | 01 |
| 如果数组是 null | if the array **is** null | 1 | 01 |
| 没有这样的下标 | there's **no such index**（单数） | 1 | 01 |
| 我先从…开始 | I'**ll start** with … | 1 | 01 |
| 不需要额外的数据结构 | we don't need **any extra** data structure | 1 | 01 |
| 把左半边扔掉 | **discard** the left half（不用 shrink） | 1 | 01 |

## 术语（这类不是语法，是知识——一次就该改掉）

| 概念 | 正确叫法 | 首次 |
|---|---|---|
| 二分查找 | **binary search**，left/right 只是 boundary pointers；不是 two pointers | 01 |
| 数组里的元素 | **values / elements / indices**，别沿用上一题的名词 | 01 |
| 读法 | O(log n) 读作 "**big O of log n**" | 01 |

## 填充词

| 词 | 次数 | 换成 |
|---|---|---|
| Yeah and … | 4 | 停顿，或 "Okay, so" |
| like | 1 | 直接删掉 |

---

## 怎么用这张表

刷完一题，把新的错误往表里加一行；已有的，次数 +1。
一周换一次「本周只盯」——挑次数最高的那条。
**术语那一栏不用盯**，它是一次性的，知道了就不会再犯。
